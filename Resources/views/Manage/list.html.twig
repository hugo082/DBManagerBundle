{% trans_default_domain 'DBManagerBundle' %}

<h3>{{ 'entity.listing'|trans }} :</h3>
{% if all is not empty %}
    <table>
        <tr>
            {% for name in all.0.obj.properties %}
                <th>{{ name }}</th>
            {% endfor %}
        </tr>
        {% for e in all %}
            <tr>
                {% for item in e.obj.vars %}
                    <td>{{ item }}</td>
                {% endfor %}

                {% if eInfo.displayElements.list.editLink and e.permissions.edit %}
                    <td><a href="{{ path('db.manager.edit', {'name':name,'id':e.obj.id})}}">{{ 'entity.action.edit'|trans }}</a></td>
                {% endif %}
                {% if eInfo.displayElements.list.removeLink and e.permissions.remove %}
                    <td><a href="{{ path('db.manager.remove', {'name':name,'id':e.obj.id})}}">{{ 'entity.action.remove'|trans }}</a></td>
                {% endif %}
            </tr>
        {% endfor %}
    </table>
{% else %}
    <div>{{ 'entity.empty'|trans }}</div>
{% endif %}